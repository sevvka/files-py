def peredacha(inf):
    with open(".vscode\papka\lololo.txt", "a", encoding='utf-8') as file:
        file.write(inf)

class dog:
    name = None
    age = None
    isstrong = None
    def setinf(self, name, age, isstrong):
        self.name = name
        self.age = age
        self.isstrong = isstrong
    def getinf(self):
        return(f"Name of the dog: {self.name}\n Age of the dog: {self.age}\n Is dog strong: {self.isstrong}") 
    def __init__(self, name, age, isstrong):
        self.setinf(name, age, isstrong)
        self.getinf()
    
dog1 = dog("mixo", 10, True)

dog2 = dog("pasha", 5, False)

dog3 = dog("salah", 3, True)

dogs = {1: dog1, 2: dog2, 3: dog3}

class cat:
    name = None
    age = None
    isfast = None
    def setcinf(self, name, age, isfast):
            self.name = name
            self.age = age
            self.isfast = isfast
    def getcinf(self):
        return(f"Name of the cat: {self.name}\n Age of the cat: {self.age}\n Is cat fast: {self.isfast}")
    def __init__(self, name, age, isfast):
        self.setcinf(name, age, isfast)
        self.getcinf()

cat1 = cat("loki", 2, True)

cat2 = cat("muddy", 19, False)

cat3 = cat("fatty", 8, False)

cats = {1: cat1, 2: cat2, 3: cat3}


while True:
    choice1 = input("записать информацию про собак или котов? 1-2. 3 to exit programm: ")
    try:
        choice1 = int(choice1)
        if choice1 == 1:
            print("dogs")
            choice2 = input("номер собаки, информацию о которой вы хотите получить? 1-3. Enter 4 to end the programm: ")
            try:
                choice2 = int(choice2)
                if choice2 == 1:
                    dogy = dogs[1]
                    info = dogy.getinf()
                    peredacha(info)
                elif choice2 == 2:
                    dogy = dogs[2]
                    info = dogy.getinf()
                    peredacha(info)
                elif choice2 == 3:
                    dogy = dogs[3]
                    info = dogy.getinf()
                    peredacha(info)
                elif choice2 == 4:
                    break
                elif choice2 > 4 or choice2 < 1:
                    print("only numbers from 1 to 3")
            except ValueError:
                print("only numbers from 1 to 3 are allowed")
        elif choice1 == 2:
            print("cats")
            choice3 = input("номер кота, информацию о котором вы хотите получить? 1-3. Enter 4 to end the programm: ")
            try:
                choice3 = int(choice3)
                if choice3 == 1:
                    kitty = cats[1]
                    info = kitty.getcinf()
                    peredacha(info)
                elif choice3 == 2:
                    kitty = cats[2]
                    info = kitty.getcinf()
                    peredacha(info)
                elif choice3 == 3:
                    kitty = cats[3]
                    info = kitty.getcinf()
                    peredacha(info)
                elif choice3 == 4:
                    break
                elif choice3 > 4 or choice3 < 1:
                    print("only numbers from 1 to 3")
            except ValueError:
                print("only numbers from 1 to 3 are allowed")
        elif choice1 > 3 or choice1 < 1:
            print("only numbers from 1 to 3 are allowed")
        elif choice1 == 3:
            print("ending programm")
            break
    except ValueError:
        print("only numbers are allowed")
